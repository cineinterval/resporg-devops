name: CI Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Unit Tests (Mock)
        run: echo "Running unit tests..."

      - name: Gatekeeping Step
        run: echo "Waiting for manual approval before merging to production (main)"

